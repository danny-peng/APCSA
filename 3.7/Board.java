public class Board {
    private String moves = "";
    // will add array to represent board once we learn arrays

    public boolean equals(Board other) {
        System.out.println("They are the same game!");
        return moves.equals(other.moves);
    }
}
